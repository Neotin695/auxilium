import 'package:flutter/Material.dart';

class AppStyle {
  static const BorderRadiusGeometry roundCorners = BorderRadius.only(
    bottomLeft: Radius.circular(25),
    bottomRight: Radius.circular(25),
  );
}
